import { Card } from 'react-bootstrap';
import styled from 'styled-components';

export const ProfileCard = styled(Card)`
  max-width: 350px;
`;

export const ProfileCardImg = styled(Card.Img)`
  margin-left: auto;
  margin-right: auto;
  width: 150px;
  padding-top: 10px;
`;